# DAG Summary — {{ stats.project_name or "dbt Project" }}

> Auto-generated by ariadne. Re-run `ariadne sync` to update.
> Last indexed: {{ generated_at }}

## Project Structure
- **Total models**: {{ stats.model_count }}
  - Staging: {{ stats.staging_count }}
  - Intermediate: {{ stats.intermediate_count }}
  - Marts: {{ stats.marts_count }}
  {% if stats.other_count %}- Other: {{ stats.other_count }}{% endif %}
- **Sources**: {{ stats.source_count }} tables across {{ stats.source_schema_count }} schemas
- **Exposures**: {{ stats.exposure_count }}
- **Tests**: {{ stats.test_count }} (~{{ stats.test_coverage_pct }}% column coverage)

## Layer Architecture
```
Sources ({{ stats.source_count }})
  └── Staging ({{ stats.staging_count }} models) — view
{% if stats.intermediate_count %}        └── Intermediate ({{ stats.intermediate_count }} models)
{% endif %}            └── Marts ({{ stats.marts_count }} models) — table
                   └── Exposures ({{ stats.exposure_count }})
```

## Naming Conventions
- **Staging**: `{{ patterns.naming.staging_pattern }}`
- **Intermediate**: `{{ patterns.naming.intermediate_pattern }}`
- **Marts**: `{{ patterns.naming.marts_pattern }}`

## Most Connected Models (by degree centrality)
{% for model in key_models -%}
| `{{ model.name }}` | {{ model.layer }} | {{ model.upstream_count }} upstream | {{ model.downstream_count }} downstream |
{% endfor %}

## Common Tags
{% if patterns.common_tags -%}
{{ patterns.common_tags | join(', ') }}
{% else -%}
None detected
{% endif %}
